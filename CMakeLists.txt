cmake_minimum_required(VERSION 2.8)
project( feature_matcher )
find_package( OpenCV REQUIRED )
add_executable( feature_matcher feature_matcher.cpp )
target_link_libraries( feature_matcher ${OpenCV_LIBS} )
